<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.keenusa.connect"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".KeenConnectApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.CoachListActivity"
            android:label="@string/title_activity_coach_list" >
        </activity>
        <activity
            android:name=".activities.AthleteListActivity"
            android:label="@string/title_activity_athlete_list" >
        </activity>
        <activity
            android:name=".activities.SessionDetailsActivity"
            android:label="@string/title_activity_session_details" >
        </activity>
        <activity
            android:name="org.keenusa.connect.activities.CheckInActivity"
            android:label="@string/title_activity_check_in" >
        </activity>
    </application>

</manifest>
